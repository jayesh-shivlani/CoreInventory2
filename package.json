{
  "name": "coreinventory-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Core Inventory IMS monorepo workspace scripts",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "lint": "npm run lint --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
