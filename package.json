{
  "name": "coreinventory2",
  "private": true,
  "version": "1.0.0",
  "description": "Core Inventory full stack workspace scripts",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
